name: actions
on:
  pull_request:
    branches:
      - develop

jobs:
  doSmthn:
    runs-on: ubuntu-latest
    steps:
      - name: rjn check
        uses: actions/checkout@v2.3.1
      - name: install
        run: npm install

      - name: test
        run: npm run test
        run: echo 'completed'
